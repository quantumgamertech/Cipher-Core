export const appConfig = Object.freeze({
  version: '0.9.0',
  codename: 'Pre-Awakening',
  telemetryProvider: 'cipher-vision',
  integrationsProvider: 'mock',
  hardwareControlEnabled: false,
  microphoneEnabled: false,
  commandExecutionEnabled: false,
  themeActionsEnabled: false,
  externalConnectionsEnabled: false,
});
